use flake

# Loads ARAP_LOCAL_DB_PASSWORD/ARAP_LOCAL_REDIS_PASSWORD from the repo-root
# .env into this shell, so `nix run --impure .#services` (flake.nix) can
# read them via builtins.getEnv without you exporting them by hand.
dotenv_if_exists .env
