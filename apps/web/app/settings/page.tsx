// Placeholder route ("/settings") — same situation as app/sessions/page.tsx:
// not in the Navbar, not documented, stub heading only.
export default function SettingsPage() {
  return (
    <main className="min-h-screen bg-bg-base px-8 pt-[84px] text-text-primary">
      <h1 className="text-[26px] font-bold">Settings</h1>
    </main>
  );
}
