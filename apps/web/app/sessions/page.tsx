// Placeholder route ("/sessions") — not documented in CLAUDE.md's "What
// Each Screen Does" section and not linked from the Navbar; a stub heading
// with no data fetching or session list yet.
export default function SessionsPage() {
  return (
    <main className="min-h-screen bg-bg-base px-8 pt-[84px] text-text-primary">
      <h1 className="text-[26px] font-bold">Sessions</h1>
    </main>
  );
}
