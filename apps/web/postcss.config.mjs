// Standard Tailwind CSS 3 PostCSS pipeline: tailwindcss expands utility
// classes/directives, autoprefixer adds vendor prefixes for browser
// compatibility.
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
